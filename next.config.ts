import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // output: 'export', // ✅ enables/disabled static export, when run with `next build || next export`
};

export default nextConfig;
